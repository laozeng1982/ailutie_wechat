                <!-- 次数选择框 -->
                <view class="middlePanelRight_row3_left">
                    <scroll-view class="middleSelectorScrollview" bindscroll="" scroll-y="true">
                        <view class="listSingleMovement" wx:for="{{countSelector}}" class='bodypart-item'>
                            <view id='{{item}}' class='bodypart'>
                                <view id='{{item}}' class="{{item==curCount?'selectedItem':''}}" value="{{item}}" class="bodypartName" bindtap="onCountTap">
                                    <view class="middleScrollItem">
                                        <view>{{item}}</view>
                                        <view>次</view>
                                    </view>
                                </view>
                            </view>
                        </view>
                    </scroll-view>
                </view>

                <!-- 重量选择框 -->
                <view class="middlePanelRight_row3_right">
                    <scroll-view class="middleSelectorScrollview" bindscroll="" scroll-y="true">
                        <view class="listSingleMovement" wx:for="{{weightSelector}}" class='bodypart-item'>
                            <view id='{{item}}' class='bodypart'>
                                <view id='{{item}}' class="{{String(item)===String(curWeight)?'selectedItem':''}}" value="{{item}}" class="bodypartName" bindtap="onWeightTap">
                                    <view class="middleScrollItem">
                                        <view>{{item}}</view>
                                        <view>{{curRecords.movementList[curSelectedMovementId-1].contents.details[0].measurement}}</view>
                                    </view>
                                </view>
                            </view>
                        </view>
                    </scroll-view>
                </view>